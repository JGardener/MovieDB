import styles from './Card.module.css'

const Card = () => {
    return(
        <div className={styles.wrapper}>
            <p>Good morning, from Card</p>
        </div>
    )
}

export default Card;